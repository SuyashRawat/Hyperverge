import { useState } from "react";
import currState from "./stateContext";
// useContext use
const State = (props) => {
    
    return (
        <>
            {/* <currState.Provider value={{ notes, addNote, deleteNote, editNote ,getNotes,setIsChecked,isChecked,filtergetNotes,isCollapsed}}>
                
                {props.children}
            </currState.Provider> */}
        </>

    );
};
export default State;